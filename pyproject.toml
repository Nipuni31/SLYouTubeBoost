[tool.ruff]
line-length = 100
target-version = "py310"
exclude = [
    ".history",
    "notebooks",
    "mlruns",
]

[tool.ruff.lint]
select = ["E", "F", "I", "W"]
ignore = ["E501"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
